useEffect(() => {
    const videoElement = document.getElementById('zoomInVideo');

    if(videoElement){

      videoElement.requestFullscreen().catch(err => {
      alert(`Error attempting to enable full-screen mode: ${err.message} (${err.name})`);;
      });
    }
  });



  switch(lang){

            case "hebrew":
                languages=languages.hebrew;
                break;
            case "russian":
                languages=languages.russian;
                break;
            case "english":
                languages=languages.elglish;
                break;
            default:
                languages=languages.hebrew;

                
// export let languages = {
//     russian: {},
//     elglish: {},
//     hebrew: {},
//   };

//   export let languages = ['hebrew', 'russian', 'english']
  
//   export const LanguageContext = React.createContext({
//     language: languages[0],//defualt value
//     setLanguage: () => {},
    
//   });


import React, {useContext} from 'react';
import LangContext from './SergiContext';
import russianText from './russianText';
import englishText from './englishText';
import hebrewText from './hebrewText';


function IsLeftToRight () {

    const { lang } = useContext(LangContext);


    
    if(lang==="english"){
        return englishText;
    }
    else if (lang==="russian")
    {}
    
    
  }

  export default IsLeftToRight;